// Postgres helper for E2E tests.
// Used sparingly -- most seeding is done via SQL fixtures.
// This helper is for per-test data setup that can't be in the shared seed.

// Placeholder: no Postgres client needed yet.
// If per-test DB seeding becomes necessary, add 'pg' to e2e/package.json
// and implement helpers here.
